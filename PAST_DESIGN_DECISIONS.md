# PAST_DESIGN_DECISIONS.md — 設計判断の経緯と没案

**最終更新**: 2026-07-13

単に最新仕様を書くのではなく、**過去に検討した案、没にした理由、現在の仕様との違い**を残す。
面白い没案は復活を検討する価値がある。

---

## 1. 体当たり判定方式

### 採用: コードベースAABB判定（`_body_contact()`）
- `PlayerMain.gd` の `_body_contact()` で毎フレーム全敵を走査
- 64×64の正方形AABBで接触判定、ずれ値で3種に分岐

### 没案: Area2Dベース判定
- SPEC B.1で計画された方式。FrontHitBox / SideHitBox を Area2D で配置
- `body_entered` / `area_entered` シグナルで検知
- **没理由**: 
  - Area2Dでは「ずれ」の計算が難しい（overlap量をArea2Dで取るのは面倒）
  - 方向入力との連携がシグナルだとタイミングずれしやすい
  - _body_contact() で直接計算する方がシンプルで調整しやすかった
- **復活検討**: Area2D方式は複数ヒットボックスの拡張性がある。ボスに特殊な弱点部位を持たせる場合は有効かも

---

## 2. ノックバックの軸制限

### 採用: X軸 or Y軸のみ（イース風）
- `_axis_knockback()` で `to_enemy` の長い軸方向だけにノックバック
- 正面・半キャラともに一軸限定

### 没案: 斜めノックバック（全方向）
- 初期は `to_enemy` そのまま（斜め）でノックバックしていた
- **没理由**: 半キャラずらしの「押し込み感」が薄れる。イース風の「軸を合わせて押し込む」感覚が失われた
- **例外**: かすり時だけは斜めノックバックを採用（意図的に「すっ飛ばし感」を出すため）

---

## 3. 半キャラずらしのノックバック量（縦横差）

### 採用: 縦横同じ量（60px）
- `PUSH_KNOCKBACK = 60`（以前は90だったが調整で60に）

### 没案: 縦は0.67倍
- 以前は縦方向のノックバックを0.67倍にしていた
- **没理由**: 縦方向で接触すると「当たり続けて見える」ため、短いノックバックだと密着が続く不自然さがあった
- 現在は縦横同じにして統一感を出している

### 没案: ノックバック量90px
- 初期の `PUSH_KNOCKBACK` は 90 だった
- **没理由**: 90だと半キャラで1回当てるだけで敵が遠くに飛びすぎ、連続ヒットの気持ちよさがなかった
- 60に下げてから「押し込んで何回も当てる」感覚が生まれた
- **注意**: コード上の `PUSH_KNOCKBACK` は現在 90 に戻っている可能性がある（未コミット変更による。要確認）

---

## 4. 敵状態（Normal/Angry/Weak）の導入

### 採用: 3状態自動遷移
- Normal → Angry: HP40%以下 or 15秒経過
- → Weak: かすり/踏み後8秒間

### 検討した代替案
- **手動トリガーのみ**: プレイヤーの特定行動でのみ状態変化（例: かすり3回でWeak）
  - 不採用理由: テンポが悪い。自動遷移の方がゲーム中の「流れ」が生まれる
- **Angry固定の敵**: 最初からAngry（半キャラ無効）の敵タイプ
  - 部分的に採用: ステージ3ボス（正面無敵）は「常時Angry的」

---

## 5. ロープの実装方式

### 採用: コードクランプ＋自動移動
- `MAT_LEFT/RIGHT/TOP/BOTTOM` でマット範囲を定義
- 端に触れたら `rope_bounce_running` で反対側まで自動移動
- Area2Dは使わない

### 没案: Area2Dロープ壁
- ロープをArea2Dで配置し、`body_entered`で跳ね返り処理
- **没理由**: 高速移動時にすり抜ける。クランプの方が確実
- ロープの見た目（`ArenaMat.tscn`のColorRect）と判定（コード）が分離しているのは、この設計判断の結果

---

## 6. QTEの設計

### 採用: ポイントがゾーンを通過中にスペースキーで成功
- `qte_core.tscn` + 独自スクリプト
- StageControllerからボスHP0で呼び出し

### 初期版: `qte_main.gd`（ルート直下）
- 同じ仕組みだが、失敗時に `get_tree().quit()` する（スタンドアロンテスト用）
- **現状**: `qte_main.gd` はルートに残っているが、実際には使われていない（StageControllerが直接qte_coreを扱う）

### 検討した代替案
- **連打QTE**: ボタンを高速で連打して成功
- **コマンドQTE**: 表示された方向キーを順番に入力
- 現在のシンプルなタイミング式が「即座に理解可能」で最もプロレスっぽいという判断

---

## 7. 移動モード（滑らか vs グリッド）

### 採用: 両方を切り替え可能（Gキー）
- デフォルトは滑らか移動
- グリッド移動は32pxステップ

### 経緯
- 元々のTopdownStarterは滑らか移動のみ
- イース風半キャラを正確にやるにはグリッド移動が理想的
- しかし滑らかの方が初見で操作しやすい
- **妥協**: 両方用意してGキーで切り替え
- **炎ダッシュ**: グリッドモード時のみの特殊状態として追加（ハイリスクハイリターン）

---

## 8. 2P対戦

### 採用: テンキー＋マウスで2P操作
- `is_player_two` フラグで入力とスプライトを分岐
- 2P専用スプライト（`m_man_gr_l1/l2`）

### 検討した代替案
- **分割画面**: SubViewportを2つにして分割表示
  - 不採用理由: トップダウンの小さいリングでは分割不要。1画面で両者見える
- **ゲームパッド2P**: コントローラーで2P
  - 未実装: input mapに定義されていない。将来追加可能

---

## 9. ステージ3ボス（ユニ帝仮面）の正面無敵

### 採用: `facing_dir_sign` で正面判定、正面からの接触は「正面ガード」
- プレイヤーだけ20ダメージ＋200pxノックバック
- 背後を取る必要がある

### 経緯
- 元々はAngry状態（半キャラ無効）だけだった
- 「ただ硬い敵」だと面白くないので「正面は攻撃が効かない」を追加
- さらに「振り向きにディレイ」を入れて、背後を取るチャンスを作った
- **バランス調整**: 最初はディレイなし（即振り向き）で無理ゲーだった→1秒ディレイで良くなった

---

## 10. 死亡演出（マスク飛び）

### 採用: `MaskFlyAway.gd` でスプライトのテクスチャを飛ばす
- ランダム方向に重力付きで飛ぶ
- 2.5倍に拡大しながら飛ぶ

### 経緯
- 元々は単にキャラが消えるだけだった
- 「マスクが飛ぶ」のはプロレスらしい演出として追加
- `global_transform` を継承して反転・スケールを保つのに苦労した

---

## 11. 面白かった没案（復活検討候補）

以下は最終的に実装されなかったが、面白い可能性のあるアイデア:

1. **タッグチーム**: 2Pが協力プレイ（対戦ではなく共闘）。挟み撃ちで半キャラが入りやすくなる
2. **ロープダッシュ攻撃**: ロープバウンド中の体当たりにダメージボーナス
3. **場外乱闘**: マット外でも戦えるモード（ロープ制限解除）
4. **観客投入**: 観客が物を投げ入れてパワーエサになる
5. **レフェリー**: リング内を歩き回るレフェリーNPC（邪魔になる）
6. **マスク剥ぎ**: 敵のマスクを剥いで弱体化させる（現在の「かすり→弱り」の元案）
