# TODO.md — 作業状態（AI引き継ぎ用）

**最終更新**: 2026-07-13 13:15
**進捗の全体像は `PROGRESS.md` を参照（現在 79%・確度中）**

このファイルは「いま何が終わっていて、次に何をするか」の唯一の管理場所。
作業の区切りごとに必ず更新してコミット・プッシュすること。

---

## 完了済み（2026-07-13 デスクトップ側AIセッション）

- [x] **KI-02修正**: 全Tween一括killの巻き込み排除。`CharacterBase.register_motion_tween()` / `kill_motion_tweens()` を導入し、ジャンプ/ロープ飛ばしのステートは自分の移動系Tweenだけをkillする
- [x] **KI-03修正**: 未使用の上ロープ矯正ロジックを削除（上下ロープはクランプのみで確定＝OQ-02はA案採用）
- [x] **KI-05修正**: QTE成功時のボス消滅を `CharacterBase._die()` に一元化（マスク飛び演出つき・二重free防止）
- [x] **KI-06修正**: ステージクリア確定後は敵フリーズ＋プレイヤー5秒無敵。次ステージ `StageController._ready()` でフリーズ解除。`set_invincible_for()` を期限管理方式に（短い無敵が長い無敵を打ち消さない）
- [x] **KI-11修正（新規発見）**: `PlayerIdleState`/`PlayerWalkState` が存在しないアクション `AttackPunch`/`AttackKick` を毎フレーム参照→エラースパム。通常攻撃遷移を削除（NON_NEGOTIABLES #1）し、グリッドモード時のNボタン＝炎ダッシュ遷移を実装（SPEC B.5.1。これまで1Pでは発動不能だった）
- [x] **KI-12修正（新規発見）**: `PlayerMain._ready()` のカメラ付け替え `add_child()` 失敗→ `call_deferred` 化
- [x] **TD-08一部対処**: パワーエサ取得時のフィードバック追加（効果色フラッシュ＋「スピード2倍！」「敵全員よわり！」ポップアップ）
- [x] **TD-10対処**: 残骸ファイル削除（`press_bar.gd`, `Scenes/qte_main.gd`, `Scenes/result_label.gd`, `Scenes/kancho.gd`）
- [x] SPEC.md 更新（§5 攻撃ボタン、§7.2 Status、B.5.1 入力仕様、B.10 Win/Lose）
- [x] KNOWN_ISSUES.md / TECHNICAL_DEBT.md / HANDOFF.md / DEVELOPMENT_LOG.md 更新
- [x] **敵状態 Angry/Weak のゲームプレイ効果を実装**（重大乖離の解消。詳細は DEVELOPMENT_LOG 2026-07-13）
  - Angry: 半キャラ無効＋プレイヤー弾き返し、移動1.35倍、攻撃1.5倍、時間起因は波状（15秒→8秒間怒り→リセット）
  - Weak: 移動0.5倍、正面でもプレイヤー無傷、敵攻撃ダメージ0、S3ボス正面ガード無効
  - 逃走ボス（S2）は怒りで加速しない

## 進行中（次のAIはここから）

- [ ] **バランス点検**: 第1弾調整済み（2026-07-13）。実プレイで要確認
  - S1: max_count 7→6、spawn_interval 11→10秒（テンポ改善）
  - S2ボスHP 110→100、S3 130→120、S4 200→180（半キャラDPS30/秒との整合）
  - **新規観点: Angry/Weak実装後の難度変化**（かすり→弱り→半キャラループ）
- [ ] **通し検証 headless**: Title/GameWrapper/StageClear/Ending ✅。QTE・StageIntroは未
- [x] **Ending.tscnパースエラー修正**: unique_idが文字列だったため数値に修正（S4クリア後遷移ブロッカー）
- [ ] **炎ダッシュの実プレイ確認**: KI-11修正でグリッドモード時のNボタン＝炎ダッシュが初めて実際に発動するようになった。エディタで遊んで挙動・バランスを確認（与/被ダメ2倍・赤モジュレート・32pxステップ移動）
- [ ] **2Pの左クリック挙動確認**: 2Pは左クリックが「自動走行/ダッシュ/炎ダッシュ」に変わった（旧: 通常攻撃Attacking）。2Pモードで動作確認
- [x] **トレーニングモード表示**: 弱り/怒り/半キャラ無効のラベル表示を追加

## 未着手（優先度順）

- [ ] OQ-04: ダウン状態の敵の復活条件（ユーザーからアルゴリズム指示待ち。現状トレーニング専用表示）
- [ ] OQ-06: エンディングの内容充実（現状は絵1枚。スタッフロール等）
- [ ] TD-09: AudioManagerプール上限（8→拡張）※大量ヒット時に音が途切れる場合のみ
- [ ] 音声アセット（clear_fanfare.ogg 等）はノートPC側からのコミット待ち。フォールバック実装済みなので無くても動く

## 実行済みテストと結果

- ✅ headless インポート（Godot 4.7）: エラーなし
- ✅ headless で `GameWrapper.tscn` を600フレーム実行: 修正前は `AttackPunch` エラースパム＋カメラ`add_child`失敗 → **修正後はエラーゼロ**（invalid UID警告と終了時リーク警告は無害）
- ⚠️ **実プレイ（目視・操作）は未実施**。特に炎ダッシュ・2P左クリック・クリア遷移まわりは人間の実プレイ確認が必要
- 既知の無害な警告: `TitleScreen.tscn` 等の invalid UID（テキストパスにフォールバックされる）

## 変更してはいけないもの

- `NON_NEGOTIABLES.md` の5項目（通常攻撃なし／半キャラずらしが勝ち筋／位置取りゲー／プロレス世界観／トップダウン）
- ロープ実装方式（コードクランプ＋自動移動。Area2Dにしない＝没案）
- ノックバックの軸制限（X or Yのみ。かすりだけ斜め）
- `_body_contact()` のコードベースAABB判定方式（Area2D化しない）
- 上下ロープはクランプのみ（今回確定）

## 運用ルール（全AI共通）

1. 実装変更したら同じ会話内で `docs/SPEC.md` を更新（`.cursor/rules/spec-sync.mdc`）
2. 変更理由を `DEVELOPMENT_LOG.md` に追記
3. 作業区切りごとに このファイル＋HANDOFF.md を更新し、コミット・プッシュ
4. コミットメッセージは日本語（docs系は `docs:` プレフィックス）
5. 基準は「実際に遊んで面白いか」
